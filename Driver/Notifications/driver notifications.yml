info:
  name: driver notifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/driver/all-notifications"
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
