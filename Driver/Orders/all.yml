info:
  name: all
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/driver/order"
  params:
    - name: filter
      value: going
      type: query
      disabled: true
    - name: slot_id
      value: "2"
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
