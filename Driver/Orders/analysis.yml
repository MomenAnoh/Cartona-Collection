info:
  name: analysis
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/driver/analysis"
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
