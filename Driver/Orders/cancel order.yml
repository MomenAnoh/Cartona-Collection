info:
  name: cancel order
  type: http
  seq: 8

http:
  method: GET
  url: /api/driver/cancel-order/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
