info:
  name: cancel order
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/driver/cancel-order/2?reason=asgrsdgsad"
  params:
    - name: reason
      value: asgrsdgsad
      type: query
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
