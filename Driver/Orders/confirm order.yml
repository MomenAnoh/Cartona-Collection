info:
  name: confirm order
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/driver/confirm-order/1"
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
