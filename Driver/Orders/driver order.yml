info:
  name: driver order
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/driver/driver-orders?type=Delivered"
  params:
    - name: type
      value: Delivered
      type: query
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
