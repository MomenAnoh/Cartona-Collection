info:
  name: slots
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/driver/slots"
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
