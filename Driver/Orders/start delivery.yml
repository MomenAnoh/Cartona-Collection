info:
  name: start delivery
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/driver/start-delivery/2"
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
