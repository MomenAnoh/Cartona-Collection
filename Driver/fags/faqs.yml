info:
  name: faqs
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/driver/faqs"
  headers:
    - name: Accept-Language
      value: ar
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
