info:
  name: add
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/driver/help"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
    data:
      - name: reason
        type: text
        value: asdgasdg
      - name: order_number
        type: text
        value: "1"
      - name: message
        type: text
        value: dsfdsfds
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
