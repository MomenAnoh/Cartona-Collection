info:
  name: add
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/driver/payment-account"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
    data:
      - name: title
        type: text
        value: adgadsfgdsf
      - name: card_number
        type: text
        value: "6546545125151"
      - name: is_primary
        type: text
        value: "1"
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
