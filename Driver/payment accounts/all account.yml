info:
  name: " all account"
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/driver/payment-account?page=2"
  params:
    - name: page
      value: "2"
      type: query
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
