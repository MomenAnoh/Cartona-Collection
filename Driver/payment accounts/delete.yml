info:
  name: delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}/api/driver/payment-account/2"
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
