info:
  name: update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{url}}/api/driver/payment-account/1"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
    data:
      - name: title
        type: text
        value: ddddddddddddddd
      - name: card_number
        type: text
        value: "66666666666666666666"
      - name: is_primary
        type: text
        value: "0"
  auth:
    type: bearer
    token: "{{driver_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: is_primary   [0,1]
