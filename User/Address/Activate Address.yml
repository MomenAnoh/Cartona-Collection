info:
  name: Activate Address
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/user/activate-address/2"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
