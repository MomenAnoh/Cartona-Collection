info:
  name: all
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/user/address"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: search
      value: Momen home
      type: query
      disabled: true
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
