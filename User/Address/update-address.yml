info:
  name: update-address
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/user/address/49"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
    data:
      - name: longitude
        type: text
        value: "47.20"
        disabled: true
      - name: latitude
        type: text
        value: "25.00"
        disabled: true
      - name: first_name
        type: text
        value: Momen home
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
