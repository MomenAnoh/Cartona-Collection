info:
  name: user-address
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/user/user-address"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
