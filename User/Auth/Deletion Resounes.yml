info:
  name: Deletion Resounes
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/api/user/account-deletion-resounses"
  headers:
    - name: Accept
      value: application/json
    - name: Accept-Language
      value: en
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
