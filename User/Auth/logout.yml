info:
  name: logout
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/user/logout"
  params:
    - name: ""
      value: ""
      type: query
      disabled: true
  body:
    type: multipart-form
    data:
      - name: "fcm_toke "
        type: text
        value: ""
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
