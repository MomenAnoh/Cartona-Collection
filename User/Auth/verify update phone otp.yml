info:
  name: verify update phone otp
  type: http
  seq: 12

http:
  method: GET
  url: "{{url}}/api/user/verify-update-phone-otp"
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
