info:
  name: delete store products form cart
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}/api/user/delete-store_product-from-cart/2"
  headers:
    - name: Accept-Language
      value: ar
  params:
    - name: ""
      value: ""
      type: query
      description: uuid if no login
      disabled: true
  body:
    type: multipart-form
    data:
      - name: guest_id
        type: text
        value: ""
        contentType: uuid if no login
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
