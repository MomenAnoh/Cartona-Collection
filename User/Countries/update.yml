info:
  name: update
  type: http
  seq: 1

http:
  method: PUT
  url: "{{url}}/api/user/user-countries/1"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
