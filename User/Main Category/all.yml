info:
  name: all
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/user/main-categories"
  headers:
    - name: Accept-Language
      value: ar
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
