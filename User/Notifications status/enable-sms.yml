info:
  name: enable-sms
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/user/enable-sms"
  headers:
    - name: Accept-Language
      value: ar
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
