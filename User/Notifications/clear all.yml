info:
  name: clear all
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/user/clear-all-messages"
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
