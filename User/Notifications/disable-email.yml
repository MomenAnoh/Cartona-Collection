info:
  name: disable-email
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/user/disable-email"
  headers:
    - name: Accept-Language
      value: ar
      disabled: true
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
