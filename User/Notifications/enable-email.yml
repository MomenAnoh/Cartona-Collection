info:
  name: enable-email
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/user/enable-email"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
