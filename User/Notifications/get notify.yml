info:
  name: get notify
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/api/user/read-notification/1"
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
