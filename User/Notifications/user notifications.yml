info:
  name: user notifications
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/user/all-notifications"
  params:
    - name: page
      value: "2"
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
