info:
  name: Cancel Order Reasons
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/user/cancel-order-reasons"
  headers:
    - name: Accept-Language
      value: ar
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
