info:
  name: all user orders
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/user/all-user-orders"
  params:
    - name: search
      value: Current
      type: query
      disabled: true
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
