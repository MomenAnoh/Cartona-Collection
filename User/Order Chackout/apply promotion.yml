info:
  name: apply promotion
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/user/redeem-promotion/3?order_id=4"
  params:
    - name: order_id
      value: "4"
      type: query
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
