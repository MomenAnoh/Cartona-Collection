info:
  name: reorder
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/user/reorder/6"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
