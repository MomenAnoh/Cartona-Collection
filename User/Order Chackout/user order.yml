info:
  name: user order
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/user/user-order/6"
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
