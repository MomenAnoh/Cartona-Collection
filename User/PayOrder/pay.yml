info:
  name: pay
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/user/pay-order"
  body:
    type: multipart-form
    data:
      - name: delivery_instructions
        type: text
        value: dzgfdsgdfgdfgg
      - name: payment_method
        type: text
        value: paypal
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
