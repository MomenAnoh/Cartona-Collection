info:
  name: store global search
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/user/store-global-search/1?search=Chicken"
  params:
    - name: search
      value: Chicken
      type: query
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
