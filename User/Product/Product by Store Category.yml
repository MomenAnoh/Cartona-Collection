info:
  name: Product by Store Category
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/user/products-by-store-category/9?search=س"
  params:
    - name: sub_category_id
      value: ""
      type: query
      disabled: true
    - name: search
      value: س
      type: query
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
