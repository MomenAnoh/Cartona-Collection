info:
  name: Products By Sub Category
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/user/products-by-sub-category/1"
  headers:
    - name: Accept
      value: application/json
    - name: Accept-Language
      value: ar
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
