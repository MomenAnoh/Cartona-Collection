info:
  name: Products By Sub Category
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/user/products-by-sub-category/1?product_id=32"
  headers:
    - name: Accept
      value: application/json
    - name: Accept-Language
      value: ar
  params:
    - name: product_id
      value: "32"
      type: query
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
