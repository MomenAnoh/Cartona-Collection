info:
  name: discount products
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/user/discount-products"
  params:
    - name: store_category_id
      value: "2"
      type: query
      disabled: true
    - name: product_sub_category_id
      value: "2"
      type: query
      disabled: true
    - name: store_id
      value: "1"
      type: query
      disabled: true
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
