info:
  name: products-by-filter
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/user/products-by-filter/?store_id=1"
  params:
    - name: store_id
      value: "1"
      type: query
      description: if you need store product
    - name: search
      value: h
      type: query
      description: search in store product
      disabled: true
    - name: store_shiping_list_id
      value: "4"
      type: query
      description: if you need product of shiping list
      disabled: true
    - name: main_order_id
      value: "8"
      type: query
      description: if your need order products
      disabled: true
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
