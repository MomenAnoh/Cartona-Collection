info:
  name: user store promotions
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/user/user-promotion"
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
