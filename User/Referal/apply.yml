info:
  name: apply
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/user/apply-referal-code"
  body:
    type: multipart-form
    data:
      - name: code
        type: text
        value: L9A5R7EU
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
