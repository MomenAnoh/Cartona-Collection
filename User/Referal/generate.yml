info:
  name: generate
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/user/generate"
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
