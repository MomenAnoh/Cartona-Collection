info:
  name: get rules
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/user/referal-rules"
  headers:
    - name: Accept-Language
      value: ar
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
