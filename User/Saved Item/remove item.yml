info:
  name: remove item
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/api/user/saved-items/2"
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
