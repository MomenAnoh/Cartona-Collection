info:
  name: delete shopping list
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}/api/user/delete-lists/1"
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
