info:
  name: Move to Cart
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/usermove-list-to-cart/1"
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
