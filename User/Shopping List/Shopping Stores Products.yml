info:
  name: Shopping Stores Products
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/user/shopping-store-products-search/2?search=p"
  headers:
    - name: Accept
      value: application/json
    - name: Accept-Language
      value: en
  params:
    - name: search
      value: p
      type: query
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
