info:
  name: add new list
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/user/add-list"
  body:
    type: multipart-form
    data:
      - name: description
        type: text
        value: ya rb
      - name: stores_id[0]
        type: text
        value: "1"
      - name: title
        type: text
        value: one list
      - name: stores_id[1]
        type: text
        value: "2"
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
