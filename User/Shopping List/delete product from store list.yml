info:
  name: delete product from store list
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{url}}/api/user/delete-product-from-list/1?store_shiping_list_id=8"
  headers:
    - name: Accept
      value: application/json
    - name: Accept-Language
      value: ar
  params:
    - name: store_shiping_list_id
      value: "8"
      type: query
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
