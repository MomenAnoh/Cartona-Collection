info:
  name: add new list
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/user/add-list"
  body:
    type: multipart-form
    data:
      - name: description
        type: text
        value: ya rb
      - name: stores_ids
        type: text
        value: "[1,2,3]"
      - name: title
        type: text
        value: one list
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
