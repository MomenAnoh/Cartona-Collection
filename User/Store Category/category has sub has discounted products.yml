info:
  name: category has sub has discounted products
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/user/categories-has-discount-products/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
