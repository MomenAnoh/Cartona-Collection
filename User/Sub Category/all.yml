info:
  name: all
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/user/sub-category"
  headers:
    - name: Accept-Language
      value: ar
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
