info:
  name: sub categories by store
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/user/sub-category/1"
  headers:
    - name: Accept-Language
      value: ar
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
