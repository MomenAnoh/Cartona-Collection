info:
  name: sub category by category
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/user/sub-category"
  params:
    - name: store_category_id
      value: "1"
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
