info:
  name: User store limit
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/user/user-store-limit"
  headers:
    - name: Accept
      value: application/json
    - name: Accept-Language
      value: ar
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
