info:
  name: get-store
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/user/stores/3"
  headers:
    - name: Accept
      value: application/json
    - name: Accept-Language
      value: ar
  params:
    - name: guest_id
      value: 550e8400-e29b-41d4-a716-446655470000
      type: query
      disabled: true
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
