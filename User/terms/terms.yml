info:
  name: terms
  type: http
  seq: 23

http:
  method: GET
  url: "{{url}}/api/user/terms"
  auth:
    type: bearer
    token: "{{user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
